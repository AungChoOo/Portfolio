---
interface Props {
    name: string;
}
const { name } = Astro.props;
---

<div
    class="col-span-1 relative overflow-hidden group aspect-square"
    style="border-radius: 50%; border: 1px solid var(--card-border); background: var(--card-bg);"
>
    <img
        src="/images/Profile.png"
        alt={name}
        class="absolute inset-0 w-full h-full object-cover transition-transform duration-500 group-hover:scale-105"
        style="object-position: center 20%"
    />
    <div
        class="absolute inset-0 bg-gradient-to-t from-black/95 via-black/40 to-transparent"
    >
    </div>
</div>
